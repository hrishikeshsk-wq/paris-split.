import {test} from 'node:test';import assert from 'node:assert/strict';import {csvCell,tripCsv,activitySummary} from '../formats.js';
test('CSV quotes commas, quotes, newlines, Unicode and prevents formula injection',()=>{assert.equal(csvCell('Café, "Paris"\nLunch'),'"Café, ""Paris""\nLunch"');assert.equal(csvCell('=1+1'),'"\'=1+1"');assert.equal(csvCell(' @SUM(A1)'),'"\' @SUM(A1)"');assert.equal(csvCell(-4.5),'-4.5');});
test('CSV exports human records and excludes invite and session secrets',()=>{const s={trip:{name:'Paris',invite_code:'SECRET'},session_token:'SESSION',members:[{id:'a',name:'Alice'}],expenses:[],splits:[],payers:[],settlements:[],activity:[{action:'added_member',member_name:'Alice',details:{name:'Bob'},created_at:'2026-09-20'}]};const csv=tripCsv(s);assert.ok(csv.includes('Added Bob'));assert.ok(csv.includes('Balance'));assert.ok(!csv.includes('SECRET'));assert.ok(!csv.includes('SESSION'));assert.equal(activitySummary(s.activity[0],s),'Added Bob');});
